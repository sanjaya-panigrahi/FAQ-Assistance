apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: spring-ai-unified-dr
  namespace: faq-assistance
spec:
  host: spring-ai-unified
  trafficPolicy:
    connectionPool:
      tcp:
        maxConnections: 50
      http:
        http1MaxPendingRequests: 50
        maxRequestsPerConnection: 20
    outlierDetection:
      consecutive5xxErrors: 5
      interval: 10s
      baseEjectionTime: 30s
      maxEjectionPercent: 50
  subsets:
    - name: v1
      labels:
        app: spring-ai-unified
---
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: langchain-unified-dr
  namespace: faq-assistance
spec:
  host: langchain-unified
  trafficPolicy:
    connectionPool:
      tcp:
        maxConnections: 40
      http:
        http1MaxPendingRequests: 40
        maxRequestsPerConnection: 20
    outlierDetection:
      consecutive5xxErrors: 5
      interval: 10s
      baseEjectionTime: 30s
      maxEjectionPercent: 50
  subsets:
    - name: v1
      labels:
        app: langchain-unified
---
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: langgraph-unified-dr
  namespace: faq-assistance
spec:
  host: langgraph-unified
  trafficPolicy:
    connectionPool:
      tcp:
        maxConnections: 40
      http:
        http1MaxPendingRequests: 40
        maxRequestsPerConnection: 20
    outlierDetection:
      consecutive5xxErrors: 5
      interval: 10s
      baseEjectionTime: 30s
      maxEjectionPercent: 50
  subsets:
    - name: v1
      labels:
        app: langgraph-unified
